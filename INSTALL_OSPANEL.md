# Установка на OSPanel (Open Server Panel)

Подробная инструкция по установке веб-приложения калькулятора себестоимости на OSPanel.

## Требования

- OSPanel установлен и запущен
- Версия PHP 7.4 или выше
- MySQL/MariaDB включен

## Шаг 1: Подготовка файлов

1. Скопируйте папку `web-app` в директорию доменов OSPanel:
   ```
   C:\OSPanel\domains\cost-calc\
   ```
   
   Или создайте папку с любым другим именем, например:
   ```
   C:\OSPanel\domains\calculator\
   ```

2. Структура должна быть такой:
   ```
   C:\OSPanel\domains\cost-calc\
   ├── backend/
   ├── frontend/
   ├── database/
   ├── config/
   ├── .htaccess
   └── ...
   ```

## Шаг 2: Создание базы данных

### Вариант 1: Через phpMyAdmin

1. Откройте OSPanel → Дополнительно → phpMyAdmin
2. Войдите (обычно логин: `root`, пароль: пустой)
3. Создайте новую базу данных:
   - Нажмите "Создать базу данных"
   - Имя: `cost_calculator_web`
   - Сравнение: `utf8mb4_unicode_ci`
   - Нажмите "Создать"
4. Выберите созданную базу данных
5. Перейдите на вкладку "Импорт"
6. Нажмите "Выбрать файл" и выберите:
   ```
   C:\OSPanel\domains\cost-calc\database\schema.sql
   ```
7. Нажмите "Вперед" (внизу страницы)

### Вариант 2: Через командную строку

1. Откройте командную строку (cmd)
2. Перейдите в директорию MySQL OSPanel:
   ```cmd
   cd C:\OSPanel\modules\database\MySQL-8.0\bin
   ```
3. Выполните:
   ```cmd
   mysql.exe -u root -p < C:\OSPanel\domains\cost-calc\database\schema.sql
   ```
   (Если пароль не установлен, просто нажмите Enter)

## Шаг 3: Настройка конфигурации

1. Откройте файл:
   ```
   C:\OSPanel\domains\cost-calc\config\config.example.php
   ```

2. Скопируйте его и переименуйте в `config.php`:
   ```
   C:\OSPanel\domains\cost-calc\config\config.php
   ```

3. Откройте `config.php` в текстовом редакторе и измените настройки базы данных:

   ```php
   <?php
   return [
       'database' => [
           'host' => 'localhost',
           'port' => 3306,
           'dbname' => 'cost_calculator_web',
           'username' => 'root',           // Обычно root для OSPanel
           'password' => '',              // Обычно пустой для OSPanel
           'charset' => 'utf8mb4'
       ],
       'app' => [
           'name' => 'Калькулятор себестоимости',
           'debug' => true,
           'timezone' => 'Europe/Moscow'
       ],
       'session' => [
           'lifetime' => 3600,
           'name' => 'cost_calc_session'
       ]
   ];
   ```

   **Важно:** Если у вас установлен пароль для MySQL в OSPanel, укажите его в поле `password`.

## Шаг 4: Миграция данных (если есть данные в SQLite)

Если у вас есть данные в SQLite базе из десктопной версии:

1. Откройте командную строку (cmd)
2. Перейдите в директорию проекта:
   ```cmd
   cd C:\OSPanel\domains\cost-calc\database
   ```
3. Убедитесь, что путь к SQLite базе правильный в файле `migrate.php`
4. Выполните:
   ```cmd
   C:\OSPanel\modules\php\PHP-7.4\php.exe migrate.php
   ```
   
   Или если PHP в PATH:
   ```cmd
   php migrate.php
   ```

   **Примечание:** Если у вас другая версия PHP, измените путь соответственно:
   - `PHP-7.4` → `PHP-8.0` или `PHP-8.1` и т.д.

## Шаг 5: Настройка виртуального хоста (опционально)

### Автоматическая настройка через OSPanel:

1. Откройте OSPanel
2. Нажмите "Настройки" → "Домены"
3. Нажмите "Добавить"
4. Заполните:
   - **Домен:** `cost-calc.local` (или любое другое имя)
   - **Папка:** `C:\OSPanel\domains\cost-calc`
   - **Версия PHP:** выберите нужную (7.4+)
5. Нажмите "Сохранить"
6. Перезапустите OSPanel

### Ручная настройка hosts файла:

1. Откройте файл hosts от имени администратора:
   ```
   C:\Windows\System32\drivers\etc\hosts
   ```
2. Добавьте строку:
   ```
   127.0.0.1    cost-calc.local
   ```
3. Сохраните файл

## Шаг 6: Проверка установки

1. Убедитесь, что OSPanel запущен (зеленый флаг)
2. Откройте браузер
3. Перейдите по адресу:
   ```
   http://cost-calc.local/frontend/login.html
   ```
   
   Или если не настраивали домен:
   ```
   http://localhost/cost-calc/frontend/login.html
   ```

4. Вы должны увидеть страницу входа

## Шаг 7: Первый запуск

1. На странице входа нажмите "Регистрация"
2. Заполните форму:
   - Имя пользователя
   - Email
   - Пароль
3. Нажмите "Зарегистрироваться"
4. После успешной регистрации войдите в систему
5. Начните работу с калькулятором!

## Решение проблем

### Ошибка "Ошибка подключения к базе данных"

**Решение:**
1. Проверьте, что MySQL запущен в OSPanel
2. Проверьте настройки в `config/config.php`
3. Проверьте, что база данных `cost_calculator_web` создана
4. Попробуйте подключиться через phpMyAdmin

### Ошибка 404 на страницах

**Решение:**
1. Проверьте, что файл `.htaccess` находится в корне `web-app`
2. Убедитесь, что в OSPanel включен mod_rewrite:
   - Настройки → Модули → Apache → mod_rewrite должен быть включен
3. Перезапустите Apache в OSPanel

### Ошибка "Call to undefined function"

**Решение:**
1. Проверьте версию PHP (должна быть 7.4+)
2. Убедитесь, что расширение PDO включено:
   - Настройки → Модули → PHP → PDO должно быть включено

### Сессии не работают

**Решение:**
1. Проверьте права на папку сессий PHP
2. В OSPanel: Настройки → PHP → найдите `session.save_path`
3. Убедитесь, что папка существует и доступна для записи

### Путь к API не работает

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Network
3. Убедитесь, что путь к API правильный в `frontend/js/api.js`
4. Проверьте, что файлы API доступны по адресу:
   ```
   http://cost-calc.local/backend/api/auth.php
   ```

## Проверка версий

### Проверка версии PHP:

1. Создайте файл `info.php` в корне проекта:
   ```php
   <?php phpinfo(); ?>
   ```
2. Откройте в браузере: `http://cost-calc.local/info.php`
3. Удалите файл после проверки (из соображений безопасности)

### Проверка версии MySQL:

1. Откройте phpMyAdmin
2. Внизу страницы будет указана версия MySQL

## Дополнительные настройки

### Изменение порта (если нужно)

Если MySQL использует другой порт:

1. Откройте `config/config.php`
2. Измените `'port' => 3306` на нужный порт
3. Проверить порт можно в OSPanel: Настройки → Модули → MySQL

### Включение отображения ошибок (для разработки)

В `config/config.php` установите:

```php
'app' => [
    'name' => 'Калькулятор себестоимости',
    'debug' => true,  // Включить отладку
    'timezone' => 'Europe/Moscow'
],
```

## Быстрая проверка

Выполните эти команды в командной строке для проверки:

```cmd
# Проверка PHP
C:\OSPanel\modules\php\PHP-7.4\php.exe -v

# Проверка MySQL
C:\OSPanel\modules\database\MySQL-8.0\bin\mysql.exe -u root -p -e "SHOW DATABASES;"
```

## Готово!

После выполнения всех шагов ваше приложение должно работать на OSPanel.

Если возникли проблемы, проверьте:
- ✅ OSPanel запущен (зеленый флаг)
- ✅ Apache запущен
- ✅ MySQL запущен
- ✅ База данных создана
- ✅ Конфигурация настроена
- ✅ Файлы на месте

Удачи! 🚀
